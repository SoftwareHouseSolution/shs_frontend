/* Prototype source: SwhSections.jsx:7 — `const A = (p) => `../../assets/${p}``
   The relative path becomes a public-root path. This is the only asset-path change in the port. */
export const A = (p) => `/assets/${p}`;
